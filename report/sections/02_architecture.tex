\section{Architecture and Infrastructure}
\label{sec:architecture}

\subsection{Lambda Architecture}

RAPID follows the Lambda Architecture because the system must support two time scales at the same time \cite{marz2015bigdata}. The batch layer reads the complete historical dataset from HDFS and computes stable views. The speed layer consumes new events from Kafka and updates Cassandra with recent threats. The serving layer exposes both types of evidence through an API and dashboard.

\begin{figure}[H]
    \centering
    \resizebox{0.92\textwidth}{!}{\input{figures/lambda_architecture.tex}}
    \caption{RAPID Lambda Architecture: HDFS stores replayable logs, Kafka carries live events, Spark computes batch and speed views, HBase and Cassandra serve results, and Flask exposes them to the dashboard.}
    \label{fig:lambda-architecture}
\end{figure}

The architectural decision is important for cybersecurity. Historical reputation prevents an IP from being treated as harmless only because it is currently quiet. Real-time alerts prevent the system from waiting for a future batch job before reacting. The API takes the maximum useful evidence from both layers and returns a decision-ready response.

\subsection{Distributed Infrastructure}

The project was deployed as a distributed student cluster rather than a single local script. This improves realism and avoids overloading one machine with Kafka, HDFS, Spark, Cassandra, HBase, API, and dashboard services. Tailscale provides stable private addresses between physical machines, while Docker Swarm provides the shared container network \cite{tailscale_docs,docker_swarm_docs}.

\begin{figure}[H]
    \centering
    \resizebox{0.88\textwidth}{!}{\input{figures/tailscale_swarm_layers.tex}}
    \caption{Infrastructure layers: Tailscale connects the physical machines, while Docker Swarm provides a shared overlay network for containers.}
    \label{fig:tailscale-swarm}
\end{figure}

\begin{table}[H]
    \centering
    \small
    \caption{Distributed service ownership.}
    \label{tab:deployment}
    \begin{tabularx}{\textwidth}{>{\bfseries}p{0.15\textwidth} p{0.24\textwidth} X}
        \toprule
        Owner & Address & Main services \\
        \midrule
        Anass & \texttt{100.73.216.115} & Kafka, Zookeeper, HDFS NameNode/DataNode, Flask API \\
        Hamza & \texttt{100.72.34.26} & Spark master and Spark worker for batch and replay work \\
        Khalid & \texttt{100.127.99.23} & Spark streaming container and real-time detectors \\
        Chawi & \texttt{100.97.208.110} & Cassandra, HBase, HBase Thrift, dashboard hosting \\
        \bottomrule
    \end{tabularx}
\end{table}

This deployment gives each layer a clear responsibility. Kafka and HDFS act as the data backbone. Spark performs computation. HBase and Cassandra store serving views. Flask is the integration point. The dashboard consumes only REST responses and does not connect directly to databases.

\begin{figure}[H]
    \centering
    \resizebox{0.88\textwidth}{!}{\input{figures/runtime_communication.tex}}
    \caption{Runtime communication between the machines and services during ingestion, processing, serving, and dashboard visualization.}
    \label{fig:runtime-communication}
\end{figure}

\subsection{Operational Evidence}

The deployment was verified with Docker and service-level checks. Figure~\ref{fig:docker-evidence} shows the running containers on the infrastructure node. Similar captures were produced for the Spark and serving nodes, proving that the project is not only theoretical but deployed as a multi-service Big Data platform.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.84\textwidth]{figures/dockerps1.png}
    \caption{Infrastructure containers on Anass' node, including Kafka, Zookeeper, HDFS, and Flask API services.}
    \label{fig:docker-evidence}
\end{figure}

\subsection{Technology Choices}

Kafka was selected because it decouples producers and streaming consumers \cite{kafka_docs}. HDFS was selected because it stores the raw dataset in a replayable form \cite{hdfs_docs}. Spark was selected because it supports both batch jobs and Structured Streaming with a shared DataFrame API \cite{spark_docs,spark_streaming_docs}. HBase was selected for historical views that are computed by batch jobs and read by the API \cite{hbase_docs}. Cassandra was selected for recent active threats because it supports high write throughput and key-based reads \cite{cassandra_docs}. Flask was selected as a lightweight REST bridge between storage systems and dashboards \cite{flask_docs}.
